# TerminalGraphingCalculator
A 3D graphing calculator that runs in the terminal. 3D parametrized equations are projected onto a 2D plane (the screen) and each "pixel" is represented using an ASCII character.
